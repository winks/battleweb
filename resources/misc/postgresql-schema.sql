CREATE TABLE bwchars (
  id varchar(255) NOT NULL,
  content text,
  updated text,
  PRIMARY KEY(id)
);

CREATE TABLE bwlists (
  id varchar(255) NOT NULL,
  content text,
  updated text,
  PRIMARY KEY(id)
);
